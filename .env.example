# Dev only (Vite proxy target)
VITE_PROXY_TARGET=http://localhost:8080

# Optional: override API base URL.
# In dev, the default (/api/v1) works with the proxy above.
# In production, set it to https://your-domain.com/api/v1 (or keep /api/v1 if you use a reverse proxy).
# VITE_API_BASE=/api/v1

# Optional: token refresh strategy (requires backend support).
# If enabled, the frontend will try to refresh once on 401 and also 60s before token expiration.
# VITE_AUTH_REFRESH=false
# VITE_AUTH_REFRESH_ENDPOINT=/auth/refresh
