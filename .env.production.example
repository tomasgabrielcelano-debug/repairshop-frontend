# Production example
# If frontend and backend are on different origins:
VITE_API_BASE=https://api.tu-dominio.com/api/v1

# If you use a reverse proxy (same origin), you can omit VITE_API_BASE and keep the default (/api/v1)
